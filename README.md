# Respaldo de una carpeta a un archivo zip con fecha y hora

Script para respaldar una carpeta a un archivo zip con fecha y hora.
Requiere incluir dentro de la carperta de la aplicacion, otra carpeta llamada: "myfiles" con algunos archivos

Elimina el tiempo de respaldo de archivos importantes si se ejecuta este script y se deja como una tarea programada